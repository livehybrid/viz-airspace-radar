{
  "name": "viz-airspace-radar-dev",
  "version": "1.0.6",
  "private": true,
  "description": "Dev tooling for the viz-airspace-radar Splunk app (jest render/config tests + Playwright e2e). Not shipped in the app package. The app itself has no build step: appserver/static/visualizations/airspace_radar/visualization.js ships as authored.",
  "scripts": {
    "test": "jest",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jest": "^29.7.0",
    "jest-canvas-mock": "^2.5.2",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFiles": [
      "jest-canvas-mock"
    ],
    "testMatch": [
      "<rootDir>/tests/**/*.test.js"
    ]
  }
}
